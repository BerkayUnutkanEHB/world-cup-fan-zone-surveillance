# Kopieer dit bestand naar ".env" en pas indien nodig aan:
#   cp .env.template .env
# Daarna: docker compose up --build

# Naam van de MongoDB-database
MONGO_DB_NAME=fan_zone_surveillance

# Poort waarop de backend draait
BACKEND_PORT=3000

# Poort waarop de frontend draait
FRONTEND_PORT=5173

# URL waarop de frontend de backend bereikt (vanuit de browser)
VITE_API_URL=http://localhost:3000
